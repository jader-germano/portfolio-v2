# LEGACY / PREVIEW ONLY
# Do not apply this file for current production ingress ownership.
# Current source of truth:
#   ~/code/pessoal/jpglabs/infrastructure/docs/current-deploy-truth.md
# Public production ingress is currently owned by Docker Traefik on the VPS.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: backend-preview-ingress
  namespace: jpglabs
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  rules:
    - host: portfolio-preview.jpglabs.com.br
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: portfolio
                port:
                  number: 80
